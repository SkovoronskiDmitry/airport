# Using jetty plugin

1. Start Rest using Maven Jetty plugin use:

```
cd airport-rest-app  
mvn jetty:run
```
2. Start Web using Maven Jetty plugin use:
   
```
cd airport-web-app  
mvn jetty:run
```